<h1>
  <%= item.isNew() ? "Create" : "Edit" %>
</h1>
<label for="item-title">Title</label>
  <input
    type="text"
    id="notebook-title"
    name="<%= (item.routesName === 'notebooks') ? "notebook" : "tag" %>[title]"
    value="<%= item.get('title') %>">
  </input>
<input type="submit" value="submit">
